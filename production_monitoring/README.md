# Production Monitoring

This project adds production observability to the deployed `auth-service`.
Sentry captures errors with safe request context, samples performance traces,
and complements the existing health endpoint. The accompanying notes explain
percentiles, alert baselines, and a practical triage framework.
